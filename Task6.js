// create a variablr type of const and assign value 
const kelvin = 0;
// create a var type of var and assign the sub value
var Celsius = kelvin - 273;
// create a var type of var for calcu equ fahrenheit and using math.module 
var fahrenheit = Math.floor(Celsius * (9 / 5) + 32);
// print the conversion using string interpolution
console.log(`The temperature is  ${Celsius}  degrees  ${fahrenheit}`);